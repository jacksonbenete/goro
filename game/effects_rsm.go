package game

// RSM and RSM2 are robr effect-table primitives backed by animated model effects.
// Goro currently renders animated map RSMs through the map/model path, but does
// not yet expose a table-driven world effect component for RSM/RSM2 entries.
